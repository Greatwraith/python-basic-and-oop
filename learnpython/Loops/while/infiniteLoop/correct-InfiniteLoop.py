def print_range(start, end):
    # Loop through the numbers from start to end
    n = start 
    
    
    # n <= end . means as long as the 'n' is not greater than the 'end'. 
    # keep running the code(which increments the number) until it reached 'end'(exact number as end)
    
    while n <= end: #  ✅  ADD <= 
        print(str(n)) # print the output of "incrementing process"
        n += 1 #incrementing process
        
        #INCREMENTING PROCESS. keep n + 1 =... (eachtime/loop )
        # until it is as exact number as 'end'
        



print_range(1, 5)



